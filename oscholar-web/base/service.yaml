apiVersion: v1
kind: Service
metadata:
  name: oscholar-web-service
  labels:
    app: oscholar-web
spec:
  selector:
    app: oscholar-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
