apiVersion: v1
kind: Service
metadata:
  name: oscholar-paper-service
  labels:
    app: oscholar-paper
spec:
  selector:
    app: oscholar-paper
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
