apiVersion: v1
kind: Service
metadata:
  name: oscholar-deploy-test-service
  labels:
    app: oscholar-deploy-test
spec:
  selector:
    app: oscholar-deploy-test
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
