apiVersion: v1
kind: Service
metadata:
  name: oscholar-user-service
  labels:
    app: oscholar-user
spec:
  selector:
    app: oscholar-user
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
